# 2025/11/26 デイリーチャレンジ

今日の問題は `node-cron` を使ったタスクスケジューリングです。

## 課題

`problem/script.js` ファイルを開き、2つの `// 問題:` セクションの指示に従ってコードを完成させてください。

目的は、`node-cron` を使って、1秒ごとにコンソールにメッセージを出力するスケジュールタスクを作成することです。

## セットアップと実行

1.  **依存関係のインストール:**
    ターミナルで `20251126` ディレクトリにいることを確認し、以下のコマンドを実行します。
    ```bash
    npm install
    ```

2.  **動作確認:**
    `script.js` を編集したら、以下のコマンドで実行を確認できます。コンソールに1秒ごとにメッセージが表示され、5秒後に停止すれば成功です。
    ```bash
    node problem/script.js
    ```

## テスト

コードが正しく書けているか確認するために、テストを実行します。

1.  **テストの実行:**
    ターミナルで以下のコマンドを実行します。
    ```bash
    npm test
    ```

2.  **期待される結果:**
    すべてのテストがパスすれば、あなたのコードは要件を満たしています。

## ヒント

- `node-cron` の `schedule` 関数は、第一引数にcron式、第二引数に実行するタスク関数を取ります。
- 1秒ごとに実行するためのcron式は `* * * * * *` です。

解答は `correct` フォルダ内にあります。
