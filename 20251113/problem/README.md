# 20251113の問題

## 問題

Todoリストを完成させてください。

`problem/script.js`を開き、以下の4つの関数の中身を実装してください。

- `saveTodos()`: TodoリストをlocalStorageに保存する関数
- `addTodo()`: 新しいTodoをリストに追加する関数
- `deleteTodo()`: Todoをリストから削除する関数
- `toggleComplete()`: Todoの完了状態を切り替える関数

ヒントはコードの中にコメントとして記載されています。

## 完成形

`correct`フォルダに完成形のコードがあります。

## テスト

テストを実行するには、`20251113` ディレクトリで次のコマンドを実行します。

```bash
npm install
npm test
```