# 問題: 来月の今日

`script.js` ファイルの `getNextMonthDate` 関数を完成させてください。
この関数は、与えられた `Date` オブジェクトの翌月の同じ日の日付を `yyyy-mm-dd` 形式の文字列で返す必要があります。

## テストの実行方法

1.  ターミナルでこのディレクトリ (`20251122`) に移動します。
2.  `npm install` を実行して、テストライブラリ `vitest` をインストールします。
3.  `npm test` を実行して、テストを実行します。
